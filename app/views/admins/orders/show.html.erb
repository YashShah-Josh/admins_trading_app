<p style="color: green"><%= notice %></p>

<%= render @order %>

<div>
  <%= link_to "Back to orders", orders_path %>

  <%= button_to "Destroy this order", admins_orders_path(@order), method: :delete %>
</div>
